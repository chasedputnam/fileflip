# Code review — Packaged media conversions

Reviewed: 2026-07-29 10:52:13
Scope: implementation against `requirements.md`, `design.md`, and `tasks.md`

## BLOCKING

- [x] `Sources/FileConvertProviders/FFmpegMediaProvider.swift:35,53-58` — Resolved: provider preflight and result acceptance now use `lstat`; only an absent path may launch and only a nonempty bounded regular node may be returned. Added passing regressions for dangling pre-existing and runner-produced symlinks.

## IMPORTANT

- [x] `Sources/packaged-media-smoke/main.swift:127-140` — Resolved: smoke now compares source byte length and SHA-256 to the certified fixture record before probing or conversion.

## Security pass

The packaged-tool locator/verifier is fail-closed, rejects unsafe paths and unknown manifest fields, verifies hashes/signatures/architecture/configuration/inventory/self-test, and never falls back to ambient tools. Release evidence is candidate-, manifest-, route-set-, revision-, platform-, timestamp-, and command-bound. No unresolved blocking or important findings remain after the fixes above.
